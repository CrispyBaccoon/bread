# RUN

- Display LOGO
- check if bread directory exist => if so continue if not ask to
- display all breadcrumbs

- display all options
  - Display breadcrumbs
  - add breadcrumb
  - solve breadcrumb
  - remove breadcrumb
